# Base URL of the Tatch API (no trailing slash).
# Leave unset to use staging. In the Vercel production project, set:
#   REACT_APP_API_BASE=https://api.tatchcakery.com/api
REACT_APP_API_BASE=https://staging-api.tatchcakery.com/api
